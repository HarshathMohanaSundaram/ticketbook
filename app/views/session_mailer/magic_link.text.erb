Hello<%= " #{@user.name}" if @user.name.present? %>,

Use the link below to sign in to Ticketbook. It expires in <%= @expires_in_minutes %>.

<%= @url %>

If you did not request this, you can ignore this email -- nobody can sign in
without the link.
